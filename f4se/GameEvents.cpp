#include "f4se/GameEvents.h"

// 
RelocPtr <BSTGlobalEvent::EventSource<ApplyColorUpdateEvent>*> g_colorUpdateDispatcher(0x03DA7440);

// 
RelocPtr <BSTGlobalEvent*> g_globalEvents(0x030DBE40);
